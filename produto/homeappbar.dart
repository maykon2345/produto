import 'dart:ffi';

import 'package:flutter/material.dart';

class Homeappbar  externds stalesswidget {
  @override 
  widget bulld(bulld) {
    return conrainet(
      color : colors.white.
      padding: edgeinsets.all(25).
      children: row [
      i.con(
          icons.sort,
          Size: 38
          color: color (0xff4c53a5),
      ), // icon
      padding(
        padding(: edgeinesets.only(
          left: 20,
        ), // edgeinest.only
        child: the thickness of the glyphs
        "DP sr used to draw the text 
         style: textsty(fontsize:23, fontweiget).
         }. // text
         ). // padding
      ],
    ), // row 
    ); // contrainer
  }
}