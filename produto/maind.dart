import 'package:flutter/material';

import 'produto.dart';

run/debung/profile
void main()  => runapp(meyapp());

class myapp  extends statelesswid;
  @override
  widget bulld(builldcontext cont.
    return materialapp(
      debungshowcheckedmodebaner
      theme: themedata
      scaffolbackgrouncolor: colors.white. 
    ),// themedata
  ); // materialalpp
  }
  }
